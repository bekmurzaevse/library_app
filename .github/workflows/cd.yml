name: Deploy

on:
  push:
    branches:
      - main

# 'EOF'
#             BRANCH_NAME="main"
#             cd /home/saylaww/library
#             git config --global --add safe.directory /home/saylaww/library
#             git remote -v
#             git fetch origin main
#             git reset --hard origin/main
#             docker-compose -f docker-compose.prod.yml pull
#             docker-compose -f docker-compose.prod.yml up -d --build
#             docker exec project_php php artisan migrate --force
#             docker exec project_php php artisan config:cache

#             docker exec project_php chmod -R 775 storage/api-docs
#             docker exec project_php chown -R saylaww:www-data storage/api-docs

#             docker exec project_php php artisan l5-swagger:generate
#           EOF


jobs:
  # test11111111 before docker pull
  deploy:
    name: 🚀 Deploy to Server
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: 🔐 Установка SSH-ключа
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: 📤 Деплой на сервер
        run: |
          ssh -o StrictHostKeyChecking=no saylaww@176.96.243.188 << "/home/saylaww/library/deploy.sh"

